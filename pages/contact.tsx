import { Center, Text } from "@chakra-ui/react";

type Props = {};

export default function Contact({}: Props) {
  return (
    <Center w="full" h="full">
      <Text>Contact</Text>
    </Center>
  );
}
